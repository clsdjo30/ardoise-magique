{% extends 'base.html.twig' %}

{% block title %}Tableau de bord - L'Ardoise Magique{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1><i class="bi bi-clipboard-check"></i> Mon Tableau de Bord</h1>
                <a href="{{ path('app_admin_ardoise_new') }}" class="btn btn-success">
                    <i class="bi bi-plus-circle"></i> Nouvelle Ardoise
                </a>
            </div>

            {% if ardoises is empty %}
                <div class="alert alert-info">
                    <h4 class="alert-heading"><i class="bi bi-info-circle"></i> Bienvenue !</h4>
                    <p>Vous n'avez pas encore créé d'ardoise. Cliquez sur le bouton "Nouvelle Ardoise" pour commencer.</p>
                    <hr>
                    <p class="mb-0">
                        <strong>Conseil :</strong> Une ardoise vous permet de créer votre menu du jour et de le partager facilement
                        sur le web, en PDF ou sur les réseaux sociaux.
                    </p>
                </div>
            {% else %}
                <div class="card">
                    <div class="card-header bg-dark text-white">
                        <h5 class="mb-0">Mes Ardoises</h5>
                    </div>
                    <div class="list-group list-group-flush">
                        {% for ardoise in ardoises %}
                            <div class="list-group-item">
                                <div class="d-flex w-100 justify-content-between align-items-center">
                                    <div>
                                        <h5 class="mb-1">
                                            {{ ardoise.titre }}
                                            {% if ardoise.isActive %}
                                                <span class="badge bg-success">Active</span>
                                            {% else %}
                                                <span class="badge bg-secondary">Inactive</span>
                                            {% endif %}
                                        </h5>
                                        <small class="text-muted">
                                            <i class="bi bi-calendar"></i> Créée le {{ ardoise.dateCreation|date('d/m/Y à H:i') }}
                                        </small>
                                    </div>
                                    <div class="btn-group">
                                        {% if ardoise.isActive %}
                                            <button type="button" class="btn btn-sm btn-info" data-bs-toggle="modal" data-bs-target="#shareModal">
                                                <i class="bi bi-share"></i> Partager
                                            </button>
                                        {% endif %}
                                        <a href="{{ path('app_admin_ardoise_edit', {id: ardoise.id}) }}" class="btn btn-sm btn-primary">
                                            <i class="bi bi-pencil"></i> Éditer
                                        </a>
                                        {% if not ardoise.isActive %}
                                            <form method="POST" action="{{ path('app_admin_ardoise_toggle_active', {id: ardoise.id}) }}" style="display: inline;">
                                                <input type="hidden" name="_token" value="{{ csrf_token('toggle-active-' ~ ardoise.id) }}">
                                                <button type="submit" class="btn btn-sm btn-outline-success">
                                                    <i class="bi bi-check-circle"></i> Activer
                                                </button>
                                            </form>
                                        {% endif %}
                                        <form method="POST" action="{{ path('app_admin_ardoise_delete', {id: ardoise.id}) }}"
                                              onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette ardoise ?');"
                                              style="display: inline;">
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete-' ~ ardoise.id) }}">
                                            <button type="submit" class="btn btn-sm btn-danger">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            {% endif %}

            <div class="card mt-4">
                <div class="card-header bg-info text-white">
                    <h5 class="mb-0"><i class="bi bi-info-circle"></i> Informations</h5>
                </div>
                <div class="card-body">
                    <p><strong>Votre URL publique :</strong> <code>{{ url('app_public_web', {slug: app.user.slug}) }}</code></p>
                    <p class="mb-0">
                        <small class="text-muted">
                            Cette URL est générée automatiquement à partir du nom de votre restaurant.
                            Partagez-la avec vos clients pour qu'ils voient votre ardoise active.
                        </small>
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>

{# Modal de Partage #}
<div class="modal fade" id="shareModal" tabindex="-1" aria-labelledby="shareModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header bg-info text-white">
                <h5 class="modal-title" id="shareModalLabel">
                    <i class="bi bi-share"></i> Partager l'ardoise active
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p class="text-muted">Utilisez ces outils pour diffuser votre ardoise sur différents canaux.</p>

                {# Lien Public #}
                <div class="mb-4">
                    <label class="form-label fw-bold">
                        <i class="bi bi-link-45deg"></i> Lien Public
                    </label>
                    <div class="input-group">
                        <input type="text" class="form-control" id="publicUrl"
                               value="{{ url('app_public_web', {slug: app.user.slug}) }}" readonly>
                        <button class="btn btn-outline-secondary" type="button" onclick="copyToClipboard('publicUrl')">
                            <i class="bi bi-clipboard"></i> Copier
                        </button>
                    </div>
                    <small class="text-muted">Partagez ce lien par email, SMS ou sur votre site web.</small>
                </div>

                <hr>

                {# Partage Rapide #}
                <h6 class="mb-3"><i class="bi bi-rocket"></i> Partage Rapide</h6>

                <div class="d-grid gap-2">
                    {# Facebook #}
                    <a href="https://www.facebook.com/sharer/sharer.php?u={{ url('app_public_web', {slug: app.user.slug})|url_encode }}"
                       target="_blank"
                       class="btn btn-primary">
                        <i class="bi bi-facebook"></i> Partager sur Facebook
                    </a>

                    {# Télécharger l'image pour Instagram #}
                    <a href="{{ path('app_public_image', {slug: app.user.slug}) }}"
                       download="ardoise-{{ 'now'|date('Y-m-d') }}.jpg"
                       class="btn btn-warning text-dark">
                        <i class="bi bi-instagram"></i> Télécharger pour Instagram (.jpg)
                    </a>

                    {# PDF #}
                    <a href="{{ path('app_public_pdf', {slug: app.user.slug}) }}"
                       target="_blank"
                       class="btn btn-danger">
                        <i class="bi bi-file-pdf"></i> Télécharger en PDF
                    </a>

                    {# Voir l'ardoise #}
                    <a href="{{ path('app_public_web', {slug: app.user.slug}) }}"
                       target="_blank"
                       class="btn btn-success">
                        <i class="bi bi-eye"></i> Voir l'ardoise publique
                    </a>
                </div>

                <div class="alert alert-info mt-3 mb-0">
                    <small>
                        <i class="bi bi-info-circle"></i>
                        <strong>Astuce Instagram :</strong> Téléchargez l'image, puis publiez-la depuis votre téléphone.
                        N'oubliez pas d'ajouter des hashtags comme #restaurantlocal #menudujouroffer
                    </small>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
function copyToClipboard(elementId) {
    const element = document.getElementById(elementId);
    element.select();
    element.setSelectionRange(0, 99999); // Pour mobile

    navigator.clipboard.writeText(element.value).then(() => {
        // Créer une notification temporaire
        const btn = event.target.closest('button');
        const originalHTML = btn.innerHTML;
        btn.innerHTML = '<i class="bi bi-check"></i> Copié !';
        btn.classList.add('btn-success');
        btn.classList.remove('btn-outline-secondary');

        setTimeout(() => {
            btn.innerHTML = originalHTML;
            btn.classList.remove('btn-success');
            btn.classList.add('btn-outline-secondary');
        }, 2000);
    });
}
</script>
{% endblock %}
